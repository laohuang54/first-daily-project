<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fth.mapper.AdminMapper">
    <update id="banUser">
        update user set status = 0,reason = #{reason} where id = #{id}
    </update>
    <update id="unbanUser" parameterType="java.lang.Integer">
        update user set status = 1 where id = #{id}
    </update>

    <select id="login" resultType="com.fth.pojo.Admin" parameterType="com.fth.dto.LoginDTO">
        select * from admin where username = #{username} and password = #{password}
    </select>
    <select id="getUser" parameterType="com.fth.dto.UserDTO" resultType="com.fth.pojo.User">
        select * from user
        <where>
            <if test="username != null and username != ''"> and username like concat('%',#{username},'%')</if>
            <if test="nickname != null and nickname != ''"> and nickname like concat('%',#{nickname},'%')</if>
            <if test="sex != null and sex != ''"> and sex = #{sex}</if>
            <if test="status != null and status != ''"> and status = #{status}</if>
            <if test="start!=null and start!=''"> and create_time >= #{start}</if>
            <if test="end!=null and end!=''"> and create_time &lt;= #{end}</if>
        </where>
    </select>
    <select id="getUserList" resultType="com.fth.pojo.User" parameterType="com.fth.dto.UserDTO">

    </select>
</mapper>
